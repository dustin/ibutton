SHELL=/bin/sh
AR=@AR@
RANLIB=@RANLIB@

DEFS=@DEFS@
CFLAGS=-I. -I../mymalloc @CFLAGS@ $(DEFS)
LDFLAGS=@LDFLAGS@ @LIBS@
RRDLIB=@RRDLIB@
POSTGRESLIB=@POSTGRESLIB@

OBJ=data.o storage-pgsql.o storage-rrd.o
PROGS=collector
PROGO=collector.o

CC=@CC@

collector: $(OBJ) collector.o
	$(CC) -o $@ $(OBJ) collector.o ../utility.o ../mymalloc/mymalloc.o \
		$(LDFLAGS) $(RRDLIB) $(POSTGRESLIB)

clean:
	rm -f $(OBJ) $(PROGO) $(PROGS)
